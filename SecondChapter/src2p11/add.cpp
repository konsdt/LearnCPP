#include "add.h"
// it is best practice, that the cpp file includes its paired header file
// this allows the compiler to catch errors at compile time instead of at
// linking time


int add(int x, int y)
{
    return x + y;
}
